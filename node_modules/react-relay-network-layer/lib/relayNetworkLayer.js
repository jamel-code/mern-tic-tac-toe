'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _queries = require('./relay/queries');

var _queries2 = _interopRequireDefault(_queries);

var _queriesBatch = require('./relay/queriesBatch');

var _queriesBatch2 = _interopRequireDefault(_queriesBatch);

var _mutation = require('./relay/mutation');

var _mutation2 = _interopRequireDefault(_mutation);

var _fetchWrapper = require('./fetchWrapper');

var _fetchWrapper2 = _interopRequireDefault(_fetchWrapper);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var RelayNetworkLayer = function () {
  function RelayNetworkLayer(middlewares, options) {
    var _this = this;

    _classCallCheck(this, RelayNetworkLayer);

    this._options = options;
    this._middlewares = Array.isArray(middlewares) ? middlewares : [middlewares];
    this._supportedOptions = [];

    this._middlewares.forEach(function (mw) {
      if (mw && mw.supports) {
        if (Array.isArray(mw.supports)) {
          var _supportedOptions;

          (_supportedOptions = _this._supportedOptions).push.apply(_supportedOptions, _toConsumableArray(mw.supports));
        } else {
          _this._supportedOptions.push(mw.supports);
        }
      }
    });

    this.supports = this.supports.bind(this);
    this.sendQueries = this.sendQueries.bind(this);
    this.sendMutation = this.sendMutation.bind(this);
    this._fetchWithMiddleware = this._fetchWithMiddleware.bind(this);
    this._isBatchQueriesDisabled = this._isBatchQueriesDisabled.bind(this);
  }

  _createClass(RelayNetworkLayer, [{
    key: 'supports',
    value: function supports() {
      var _this2 = this;

      for (var _len = arguments.length, options = Array(_len), _key = 0; _key < _len; _key++) {
        options[_key] = arguments[_key];
      }

      return options.every(function (option) {
        return _this2._supportedOptions.indexOf(option) !== -1;
      });
    }
  }, {
    key: 'sendQueries',
    value: function sendQueries(requests) {
      if (requests.length > 1 && !this._isBatchQueriesDisabled()) {
        return (0, _queriesBatch2.default)(requests, this._fetchWithMiddleware);
      }

      return (0, _queries2.default)(requests, this._fetchWithMiddleware);
    }
  }, {
    key: 'sendMutation',
    value: function sendMutation(request) {
      return (0, _mutation2.default)(request, this._fetchWithMiddleware);
    }
  }, {
    key: '_fetchWithMiddleware',
    value: function _fetchWithMiddleware(req) {
      return (0, _fetchWrapper2.default)(req, this._middlewares);
    }
  }, {
    key: '_isBatchQueriesDisabled',
    value: function _isBatchQueriesDisabled() {
      return this._options && this._options.disableBatchQuery;
    }
  }]);

  return RelayNetworkLayer;
}();

exports.default = RelayNetworkLayer;